{
  "name": "prettier-plugin-css-singleline",
  "version": "0.1.2",
  "private": false,
  "type": "module",
  "files": [
    "index.mjs"
  ],
  "main": "./index.mjs",
  "exports": {
    ".": "./index.mjs"
  },
  "peerDependencies": {
    "prettier": "^3.0.0"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "format": "prettier --write",
    "test": "vitest --run",
    "test:watch": "vitest",
    "release:patch": "npm version patch && git push --follow-tags",
    "release:minor": "npm version minor && git push --follow-tags",
    "release:major": "npm version major && git push --follow-tags"
  }
}
